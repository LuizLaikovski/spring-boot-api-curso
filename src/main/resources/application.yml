spring:
  application:
    name: luiz-laikovski
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/curso-java-spring-boot?useTimeZone=true&serverTimeZone=UTC
    username: SEU_USUARIO
    password: SUA_SENHA
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false